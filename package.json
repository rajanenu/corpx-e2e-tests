{
  "name": "corpx-e2e-tests",
  "version": "0.1.0",
  "private": true,
  "description": "Two-user end-to-end regression suite for CorpX",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "test:report": "playwright show-report",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.53.0",
    "@types/node": "^25.5.0",
    "dotenv": "^16.4.5",
    "typescript": "^5.6.3"
  }
}
